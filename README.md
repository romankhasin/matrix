# matrix

Ранкер площадок для медиаплана: скоринг рекламных площадок по стоимости конверсии (CPA), качеству постклика и CPM, с настраиваемыми весами.

## Структура

- `dashboard/index.html` — интерактивный дашборд (слайдеры весов, рейтинг площадок)
- `scripts/fetch_metrika.py` — выгрузка визитов/отказов/времени на сайте/конверсий из Yandex Metrika Reporting API
- `scripts/extract_bi_deals.py` — извлечение целевых конверсий (сделок) из BI-выгрузки клиента
- `scripts/normalize_platforms.py` — словарь нормализации названий площадок (одна и та же площадка часто по-разному называется в разных выгрузках)
- `scripts/merge_data.py`, `scripts/build_merged_august.py` — сведение расходов + Метрики + BI в единую таблицу
- `data/` — исходные и промежуточные данные (⚠️ содержит реальные цифры клиента — репозиторий публичный)

## Статус

- ✅ BI-конверсии за август 2026 обработаны (фильтр: сделки, модель атрибуции «Участие»)
- ✅ Расходы за август 2026 подключены (частично — только площадки, покрытые файлом "Ежемесячная статистика MI // Level")
- ⏳ Yandex Metrika API — токен есть, ждём доступ (постклик-метрики: отказы, время на сайте)
- ⏳ Расходы на прямые размещения (Cian, Novostroy-семья и др.) — судя по всему, в отдельном источнике, который пока не подключён

## Логика скоринга

Score = взвешенная сумма нормализованных метрик (0–100):
- **CPA** — стоимость конверсии (звонки/встречи/сделки из BI)
- **Постклик** — отказы + время на сайте (Яндекс.Метрика)
- **CPM** — стоимость 1000 показов

Веса настраиваются в дашборде.
